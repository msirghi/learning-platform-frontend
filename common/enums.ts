export enum AlertType {
  ERROR = 'error',
  WARNING = 'warning',
  INFO = 'info',
  SUCCESS = 'success'
}

export enum RegisterStep {
  REGISTER = 'register',
  SUCCESS = 'sucess'
}

export enum AuthPage {
  REGISTER = 'register',
  LOGIN = 'login',
  UNKNOWN = 'unknown'
}
